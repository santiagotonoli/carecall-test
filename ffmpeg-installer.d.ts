declare module '@ffmpeg-installer/linux-x64' {
    export const path: string;
    const defaultExport = { path };
    export default defaultExport;
}
  
declare module '@ffmpeg-installer/ffmpeg' {
    export const path: string;
    const defaultExport = { path };
    export default defaultExport;
}